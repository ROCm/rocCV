@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Re-find transitive dependencies so the imported target works
find_dependency(HIP)
find_dependency(OpenMP COMPONENTS CXX)

include("${CMAKE_CURRENT_LIST_DIR}/roccvTargets.cmake")

check_required_components(roccv)